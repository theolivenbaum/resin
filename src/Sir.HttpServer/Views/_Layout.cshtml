<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title &#484;&#485;</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="/css/theme.css">
</head>
<body>
    <a href="/" title="Did you &#484;&#485;?"><h1 class="logo">&#484;&#485;<span style="font-size:0.7em;"></span></h1></a>
    <div>
        @RenderBody()
    </div>
    <footer>
        <div>
            <p>Independent, non-tracking search engine powered by <a href="https://github.com/kreeben/resin">Resin</a>.</p>
            @if (ViewData["doc_count"] != null)
            {
                <p>Index size: @ViewData["doc_count"] documents</p>
            }
            @if (ViewData["last_processed_url"] != null)
            {
                <p>Last crawled: <a href="@ViewData["last_processed_url"]">@ViewData["last_processed_title"]</a></p>
            }
            <p>Version v0.3a</p>
            <p><strong>Free services:</strong> <a href="/queryparser?collection=www&q=trump%20tower&fields=title&fields=body&skip=0&take=10&and=and">Create model</a> | <a href="/submit/page">Submit URL</a></p>
        </div>
    </footer>
</body>
</html>