@{
    Layout = "~/Views/_Layout.cshtml";
}
@{ 
    var searchLink = $"{Context.Request.Path.Value.ToLower().Replace("/crawl", "/search")}{Context.Request.QueryString}";
}
    <form action="/crawl/" method="post">
        <div class="blog-wrapper">
            <h2>You want to crawl the crawl?</h2>
            <p>Choose between three different ways to crawl <a href="@searchLink">your result</a>:</p>
            <p>
                <label><input type="radio" name="job" value="fci" /> <strong>Fetch WARC files and index</strong> their content.</label>
            </p>
            <p>
                <label><input type="radio" name="job" value="ei" /> <strong>Extract from WARC files and index</strong> those resources referenced in your result.</label>
            </p>
            <p>
                <label><input type="radio" name="job" value="eri" /> <strong>Crawl and index</strong> those resources referenced in your result.</label>
            </p>
            <p><input type="text" placeholder="Name your collection" name="collection" /><input type="submit" /></p>
            <p>We will create a searchable document collection from the crawled data. You will reference this name when reading, writing and querying. 
            You'll be able to read your data:</p>
            <p><h3>With a browser</h3> HTTP GET text/html<br /> <strong>crawlcrawler.com/?collection=[Your_Collection]</strong></p>
            <p><h3>With any HTTP client</h3> HTTP GET application/json <br /><strong>crawlcrawler.com/query?collection=[Your_Collection]&q=[Your_Query]&field=title&field=description</strong></p>
        </div>
    </form>